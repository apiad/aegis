"""Built-in aegis workflows.

Names are registered by importing the relevant submodule when listed
in `.aegis.yaml`'s top-level `workflows:` field. See
`aegis.config.yaml_loader.register_builtins`.
"""
